from esys.escript import util
from esys.escript import Vector, Solution, kronecker, interpolate, FunctionOnBoundary, \
    matrix_mult, whereZero, sup, integrate, trace, symmetric, inner, \
    sqrt, inf, length
import numpy as np

